name: Deploy Web App to GitHub Pages

on:
  push:
    branches: [main]
    paths: ['apps/web/**']  # Only deploy when web app changes
  workflow_run:
    workflows: ["Daily Creative Prompt Generation"]  # Deploy after daily prompts update
    types: [completed]
  workflow_dispatch:  # Manual trigger for testing

# Required for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Prevent concurrent deployments
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: 'apps/web/package-lock.json'

      - name: Install dependencies
        working-directory: ./apps/web
        run: npm ci

      - name: Build app
        working-directory: ./apps/web
        env:
          VITE_PROMPT_URL: 'https://estrange-prompt-server.kalley-powell.workers.dev/prompt'
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: './apps/web/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
